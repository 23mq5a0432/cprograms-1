#include<stdio.h>
void main()
{
    int n,sum,rem;
    printf("enter a value:");
    scanf("%d",&n);
    sum=0;
    while(n>0)
    {
        rem=n%10;
        sum=sum+rem;
        n=n/10;
    }
    printf("the sum of all  %d",sum);
}
